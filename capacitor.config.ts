import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.dancingquokkabop',
  appName: 'dancing-quokka-bop',
  webDir: 'dist'
};

export default config;
